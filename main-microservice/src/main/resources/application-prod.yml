spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
springdoc:
  api-docs:
    enabled: false
management:
  endpoint:
    health:
      show-details: NEVER
  endpoints:
    web:
      exposure:
        include:
          - health
